<br>
  <div class="col-md-5 col-md-offset-3" style="margin-top:40px; background:#FFFFFF; box-shadow:0px 0px 10px #666666; border:1px solid #CCCCCC;opacity: 0.8;filter:alpha(opacity=80);">
	<% user = User.find_by_id(session[:user_id]) %>
		<% allpoll = Poll.all %>

		<center><h1 style="margin-left:5px"> Poll </h1></center>
		<div class ="line"></div>
		<br>
		<div>
			<table class="table table-hover table-condensed" style="margin-left:5px">
			    <thead>
			        <tr>
			            <th>Question</th>
			            <th>Link</th>
			        </tr>
		    	</thead>
				<% allpoll.each do |item| %>
					<tbody>
						<tr>
							<td>
								<%= item.question %>
							</td>
							<td>
					            <%= link_to "View result", {:action => "view_result", :poll_id => item.id} %>
					            <br>
								<%= link_to "Vote", {:action => "vote", :poll_id => item.id, :user_id => session[:user_id]} %> 
						 	 </td>
						</tr>
					</tbody>
				<% end %>
				<tfoot>
				 
				</tfoot>
			</table>
		</div></div>
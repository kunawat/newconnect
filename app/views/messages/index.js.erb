// function chatObj(){ 
//     this.render = function(){ 
//       var chatbox = document.getElementById('chatbox'); 
//       chatbox.style.right = "0px"; 
//       chatbox.style.bottom = "10px"; 
//       chatbox.style.display = "block"; 
//       // put dynamic stuff in the box here using innerHTML property if needed 
//       // Usually an Ajax request to get the specific chat brough into the window  
//     } 
//     this.ok = function(){ 
//       document.getElementById('chatbox').style.display = "none"; 
//       document.getElementById('chatbox').style.display = "none"; 
//     } 
//   } 
//   var chat = new chatObj();

// var chatbox = document.getElementById('chatbox');
// chatbox.style.right = "0px";
// chatbox.style.bottom = "10px";
// chatbox.style.display = "block";
var new_chat_box = document.createElement('div');
new_chat_box.id = "chat_tp_" + "<%=@receiver.id%>"

var x= $("#chats_wrapper").children().length;

x=x+1;
console.log(x);
x=x*260;
console.log(x);
$("#chats_wrapper").css({width: x});
$("#chats_wrapper").append(new_chat_box);
$("#chat_tp_"+ "<%=@receiver.id%>").html("<%= escape_javascript(render('chatbox')) %>");
$("#chat_tp_"+ "<%=@receiver.id%>").css({ bottom: "10", display: "block"});
$("#chat_" + "<%= @receiver.id%>").scrollTop($("#chat_"+ "<%= @receiver.id%>")[0].scrollHeight);
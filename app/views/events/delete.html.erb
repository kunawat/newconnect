<%#= link_to"Back to Calendar", {controller: "calendar", action: "show", :class => 'back-link'}, class: "btn btn-primary" %>

 <div class="col-md-5 col-md-offset-3" style="margin-top:40px; background:#FFFFFF; box-shadow:0px 0px 10px #666666; border:1px solid #CCCCCC;opacity: 0.8;filter:alpha(opacity=80);"> 
	  
	<center><h2>Delete Event</h2></center>
	 <div class="line"> </div>	<h2>Delete Event</h2>
	<%= form_for(:event, :url => {:action => 'destroy', :id => @event.id}) do |f|%>
	
	<p>Are you sure you want to delete this event?</p>
	<p class="reference-name"><%= @event.name %></p>
	<div class="form-buttons"> 
		<%= f.submit("Event delete", :class => 'btn btn-primary')%>
	</div>
	<% end %>
</div>	
